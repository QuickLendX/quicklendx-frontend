# Sentry DSN for error tracking. When empty or unset, Sentry is disabled.
# Format: https://<key>@<org>.ingest.sentry.io/<project>
SENTRY_DSN=

# App-wide default locale (see lib/i18n/messages.ts for the list of
# available locales). Unset or unrecognized values fall back to "en".
DEFAULT_LOCALE=
